---
title: CI & Hooks
description: Integrate jscpd into your CI pipeline and git hooks.
navigation:
  icon: i-lucide-git-pull-request
seo:
  title: CI & Pre-Commit Hooks
  description: Enforce duplication thresholds in CI pipelines and pre-commit hooks to catch copy/pasted code before it reaches the main branch.
---

jscpd can enforce duplication thresholds in CI pipelines and as a local pre-commit hook — catching copy/pasted code before it reaches the main branch.

- **[Use jscpd in CI](/ci-and-hooks/ci)** — GitHub Actions, GitLab CI, and generic CI integration
- **[Use jscpd in pre-commit hooks](/ci-and-hooks/pre-commit)** — pre-commit framework, Husky, and manual git hooks