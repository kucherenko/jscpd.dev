---
title: Cross-Format Detection
description: How jscpd detects duplicates across Svelte, Astro, Vue, and Markdown formats — vs other tools.
navigation:
  icon: i-lucide-layers
seo:
  title: Cross-Format Detection Benchmarks — jscpd
  description: jscpd is the only CPD tool that detects duplicates by embedded language across Svelte, Astro, Vue, and Markdown files.
---

Multi-file component formats like `.svelte`, `.astro`, and `.vue` embed template, script, and style blocks in a single file. Markdown files can contain embedded code blocks (YAML frontmatter, TypeScript, Python). Effective CPD tools must parse these formats to detect duplicates both **within** the same format and **across** formats.

<BenchmarkCrossFormat />

## Key Findings

jscpd variants are the **only** tools that detect duplicates by embedded language — identifying matching TypeScript code blocks, Python code blocks, and YAML frontmatter separately within Markdown. All other tools treat Markdown as flat text, matching prose content rather than code.

- **jscpd variants** parse component files into separate language sections (CSS, template, script) and match within each. This enables cross-format detection — the same CSS block appearing in both `.svelte` and `.astro` files is found.
- **Duplo** finds 8 cross-format Svelte↔Astro clones but as raw text matches (5–30 lines each), without distinguishing which language section is duplicated.
- **Simian** detects overlaps but reports them as aggregate text blocks, making it impossible to attribute which part of a multi-format component is duplicated.
- **PMD CPD** cannot detect cross-format duplicates at all — it processes each language independently and has no concept of component file structure.
- **Fallow** only analyzes TypeScript/JavaScript files, so it misses all CSS, template, and Markdown duplicates across formats.

## Markdown Embedded-Code Detection

Markdown files `file3.md` and `file4.md` in the test fixtures contain identical TypeScript and Python code blocks inside fenced code regions, plus matching YAML frontmatter. This tests whether tools can detect duplication inside embedded language blocks.

jscpd variants are the **only** tools that detect duplicates by embedded language within Markdown — identifying matching TypeScript code blocks, Python code blocks, and YAML frontmatter separately. All other tools treat Markdown as flat text, matching prose content rather than embedded code.

## Methodology

- **Target:** `fixtures/` directory — 547 files, 21,645 lines across 150+ language formats
- **Within-format clones:** Duplicates detected within the same file type (e.g., Svelte→Svelte)
- **Cross-format clones:** Duplicates detected across different file types (e.g., Svelte↔Astro)
- **Embedded-code detection:** Duplicates found inside embedded language blocks within multi-language files
- **Hardware:** Apple Silicon (Darwin arm64)

Full methodology details are in the [jscpd repository](https://github.com/kucherenko/jscpd/blob/master/benchmark/BENCHMARK.md).